const mongoose = require('mongoose')

const thoughtScema = new mongoose.Schema({
    thoughtText: { type: String, required: true},
    createdAt: 
    username: {type: String, required: true},
    reactions: 
})